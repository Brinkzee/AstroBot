# 开发过程手记 (Dev Notes) - 第六章：工作流核心分流器正式版与确定性退款子流程

本文档用于追踪并留痕第六章（指代消解与口语归一化、意图识别 Prompt 四件套与置信度级联、核心场景检索侧 Query 扩写、退款/售后确定性子流程、订单槽位中断挂起与卡片回填、退款表单交互闭环）整个开发周期的关键决策、阶段产出、沟通纠偏与技术踩坑记录。

---

## 阶段 0: Brainstorm 定稿

- **用户关键原话**:
  > "我要用 Superpowers 模式把客服系统里占位的分流器做成正式版,这是 Workflow 里最关键的节点。"
  > "1. 指代消解:用 LLM 结合对话历史,把「它能退吗」这种带指代的话补全成一句不依赖上下文也能看懂的完整问题;问题本身已完整、指代已明确的,原样透传,不强行改写"
  > "2. Query 改写:口语模糊问法归一成标准问法,和指代消解一并处理"
  > "3. Query 扩写:把一句问题泛化成多条侧重点不同的检索查询(强制 JSON 输出,字段就 queries 一个数组),几条一起检索再去重合并;只对退款退货、售后这类高频、容错低的核心场景做,简单 FAQ 不扩;扩写放检索侧现查现用,库里知识只留一份,不在入库侧拆存多份"
  > "4. 意图识别用 LLM prompt 路线,不训分类模型,prompt 按四件套写: 把七类意图(物流、订单、商品咨询、退款退货、售后、投诉、闲聊)枚举成选择题让模型选; 强制 JSON 输出,字段就 intent 和 confidence 两个; 给几个边界 few-shot 样例; 留「其他」兜底类,拿不准就归它,别硬塞进业务意图"
  > "5. 分流精化:退款退货和售后走一条确定性子流程——先拿订单数据、再做 Query 扩写并强制检索政策条款,最后只把「这一单能不能退」交给主力 Agent 判断"
  > "6. 槽位处理:缺订单号不让模型猜,执行阶段弹订单选择器让用户点选回填;退款原因不追问,用户提交退款单时从固定类目里自选;需求澄清留在主力 Agent 里做,意图识别只判「要干什么」"
  > "7. 前端配套:订单选择器做成聊天流里可点选的订单卡片,点选后自动回填、流程接着走;退款单是一个简单表单,退款原因从固定类目里下拉选"
  > 订单槽位中断回填机制确认：“方案A”（状态流事件驱动 + 自动发送）
  > 退款表单触发时机与类目确认：“确认”（Agent 判定满足退款时下发 apply_refund 动作，弹窗提供固定类目，提交至 /api/tickets）
  > 架构方案选择：“方案 1”（顶层图显式拆分子流程节点：订单提取/缺单拦截 -> Query 扩写与政策检索 -> 主力 Agent 裁决）
  > 模块 1~4 详细设计呈现阶段：“确认”

- **关键产出**:
  - Spec 设计规范路径: `docs/superpowers/specs/2026-09-11-router-refinement-design.md`
  - 核心架构决议:
    1. **显式子流程图拓扑（方案 1）**：LangGraph 主拓扑中引入 `refund_order_check`、`emit_order_selector`、`refund_expansion_retrieval`，形成严格确定性的退款处理水线；
    2. **指代消解与口语改写合一**：结合会话上下文精准补全「它能退吗」为完整自包含问题；严格实施「原样透传保护」，完整无歧义句子与寒暄不强行改写；
    3. **意图识别 Prompt 四件套**：8 分类枚举选择题（7 业务 + 1 「其他」兜底）、强 JSON（含 `confidence`）、边界 few-shot、大模型保底加小模型置信度级联降级策略；
    4. **核心场景检索侧 Query 扩写**：仅对退款退货/售后触发，LLM 输出 `{"queries": [...]}`，并发检索、内容哈希去重与分数重排，知识库物理单份存储；
    5. **订单槽位中断回填闭环（方案 A）**：缺单号时工作流在 `refund_order_check` 拦截，下发 `order_selector` 事件及 Mock 订单卡片，前端点选后自动发送 `退款订单: {order_id}` 续接；
    6. **主力 Agent 职责专一化**：预装真实订单数据与政策证据，Agent 仅专注裁决「这一单能不能退」，杜绝多余工具调用与退款原因追问；满足退款条件触发 `apply_refund`，前端弹窗自选固定类目提交工单。

- **用户拒绝或纠偏**:
  - 确认采用方案 A（状态流驱动 + 自动发送），避免 LangGraph 内部 interrupt/resume 对既有流式输出与消息持久化机制造成侵入；
  - 明确退款表单无需用户手打长文本，复用 `/api/tickets` 接口并限定固定类目下拉选。

- **翻车与返工**:
  - 无。

---

## 阶段 1: Plan 评审通过

- **用户关键原话**:
  > Spec 评审阶段：“确认”

- **关键产出**:
  - 实施计划文件路径: `docs/superpowers/plans/2026-09-11-router-refinement.md`
  - Git Commit: `4362fec` (`docs(plan): add implementation plan for ch06 router refinement`)
  - 任务追踪工件: `task.md`
  - Planning 审查工件: `implementation_plan.md`
  - 10 大任务拆解与 SDD 执行机制：
    - Task 1: 扩展工作流 State 状态与初始化配置 (`app/services/workflow/state.py`)
    - Task 2: 指代消解与口语归一化合并节点 (含透传保护) (`app/services/workflow/nodes/pre_nodes.py`)
    - Task 3: 意图识别 Prompt 四件套与置信度级联降级 (8 分类、强 JSON、few-shot、兜底)
    - Task 4: 确定性退款子流程之订单槽位提取与缺单挂起 (`refund_order_check_node`)
    - Task 5: 核心场景检索侧 Query 扩写与多路政策召回 (`refund_expansion_retrieval_node`)
    - Task 6: 退款子流程与主力 Agent 专注裁决集成 (`main_agent_node`, `suggested_actions`)
    - Task 7: 组装正式版分流路由器与全图拓扑 (`router.py`, `engine.py`)
    - Task 8: ChatService 与 SSE 流式事件协议串联适配 (`stream_chat`, `order_selector`)
    - Task 9: 前端 Vibe Coding 交互配套 (订单选择卡片与退款单表单)
    - Task 10: 综合端到端验收套件与全量回归验证 (`tests/test_ch06_acceptance.py`)

- **用户拒绝或纠偏**:
  - 暂无。

- **翻车与返工**:
  - 无。

---

## 阶段 2: Task 1 完成（扩展工作流 State 状态与初始化配置）

- **用户关键原话**:
  > 执行模式选择阶段：“Subagent-Driven”

- **关键产出**:
  - 代码实现: `app/services/workflow/state.py`
  - 单元测试: `tests/test_workflow_state.py`（3 项测试全部 PASS，全量 253 项回归测试 100% 通过）
  - Git Commit: `ec4a7cfcd574e0565260801b8b1f0e10771b8c3e` (`feat(workflow): extend AgentWorkflowState with order slots and confidence`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 在 `AgentWorkflowState` TypedDict 中扩展新增 `confidence`（意图置信度）、`order_id`（目标订单编号）、`order_data`（预取订单详情）、`suggested_orders`（缺失订单时 Mock 候选卡片列表）4 大核心槽位字段；
    2. `create_initial_state` 函数完成缺省值对称初始化（全量设为 `None`），保证既有节点与新节点无缝兼容，向后兼容性良好；
    3. 严格践行 TDD 流程：先补全单测断言确认 RED，再实现 TypedDict 与函数确认 GREEN，最后执行全局 253 项测试通过。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 3: Task 2 完成（指代消解与口语归一化合并节点，含透传保护）

- **用户关键原话**:
  > "1. 指代消解:用 LLM 结合对话历史,把「它能退吗」这种带指代的话补全成一句不依赖上下文也能看懂的完整问题;问题本身已完整、指代已明确的,原样透传,不强行改写"
  > "2. Query 改写:口语模糊问法归一成标准问法,和指代消解一并处理"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/pre_nodes.py`
  - 单元测试: `tests/test_workflow_coref_rewrite.py`（6 项测试全部 PASS，全量 259 项回归测试 100% 通过）
  - Git Commit: `4786407106dc1e16bd8c3e4efd7328dcc70b7d4f` (`feat(workflow): implement coreference rewrite node with passthrough protection`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 构建 `COREFERENCE_REWRITE_SYSTEM_PROMPT`，融合多轮对话历史精准消解代词（如「它能退吗」结合历史订单补全为独立自包含提问）；
    2. 将口语化、含混或方言表达（如「货到哪了老铁」）归一化为标准的业务提问；
    3. 严密构建双层「原样透传保护 (Hard Gate)」机制：针对纯寒暄与自包含完整句子，在代码规则层即时短路零 Token 返回；在进入大模型时以硬约束严禁过度改写或擅自添加多余修饰；
    4. 设计 `_AwaitableDict` 机制，优雅兼顾了旧单元测试的同步字典直接读取与 LangGraph 工作流引擎的异步 `await` 调用，完美实现零破坏平滑演进。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 审查包生成时，PowerShell 对 `..` 范围解析报错，通过显式加引号 `"488057a..4786407"` 恢复正常。

---

## 阶段 4: Task 3 完成（意图识别 Prompt 四件套与置信度级联降级）

- **用户关键原话**:
  > "4. 意图识别用 LLM prompt 路线,不训分类模型,prompt 按四件套写: 把七类意图(物流、订单、商品咨询、退款退货、售后、投诉、闲聊)枚举成选择题让模型选; 强制 JSON 输出,字段就 intent 和 confidence 两个; 给几个边界 few-shot 样例; 留「其他」兜底类,拿不准就归它,别硬塞进业务意图"
  > "技术栈: 意图识别默认配够格的大模型先把准确率做上去,成本吃紧时再走降级路:小模型给判断和置信度,置信度低才升级大模型重判一次"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/pre_nodes.py`
  - 单元测试: `tests/test_workflow_intent_four_elements.py`（11 项测试全部 PASS，全量 270 项回归测试 100% 通过）
  - Git Commit: `5e8647752a24404d5bb901416bc39026fd3740f9` (`feat(workflow): implement intent recognition prompt four elements with confidence cascade`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 构建 `INTENT_FOUR_ELEMENTS_PROMPT`，集齐 8 分类枚举选择题（7 业务 + 1 兜底）、强制 JSON 输出（含 `confidence` 浮点数）、6 大边界 few-shot 样例（售后 vs 退款、催发货 vs 催退款、投诉、怪问题）与「其他」兜底保护规则；
    2. 实现生产级双层模型置信度级联降级策略：小模型高置信度（`>=0.85`）且为有效业务意图时直接采纳直通返回；低置信度（`<0.85`）或初判为「其他」或解析异常时，自动唤醒主力大模型重判与兜底把关；
    3. 新增 `other_fallback_node` 友好引导出口，对超出商城业务范围的离群提问直接给出专业亲和说明并推荐转人工客服 `["transfer_agent"]`；
    4. 具备高度健壮的容错机制：JSON 解析异常或模型故障时自动降级至 `intent="其他"`, `confidence=0.0`，向后兼容历史测试数据。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 5: Task 4 完成（确定性退款子流程之订单槽位提取与缺单挂起）

- **用户关键原话**:
  > "5. 分流精化:退款退货和售后走一条确定性子流程——先拿订单数据、再做 Query 扩写并强制检索政策条款,最后只把「这一单能不能退」交给主力 Agent 判断"
  > "6. 槽位处理:缺订单号不让模型猜,执行阶段弹订单选择器让用户点选回填;退款原因不追问,用户提交退款单时从固定类目里自选;需求澄清留在主力 Agent 里做,意图识别只判「要干什么」"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/refund_nodes.py`, `app/services/workflow/nodes/__init__.py`
  - 单元测试: `tests/test_workflow_refund_nodes.py`（9 项测试全部 PASS，全量 279 项回归测试 100% 通过）
  - Git Commit: `d5ea523aa1eecb7db84d23bb0540789c8763c209` (`feat(workflow): implement refund order check and slot interruption node`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 确立订单槽位三级提取策略（`state.order_id` -> `resolved_query`/`input_query` 正则前缀与数字匹配 -> `messages` 历史倒序用户消息过滤追溯），并用正则负向预查排除金额（3000元）与年份（2026年）等业务数字干扰；
    2. 命中有效单号时优先从 `MOCK_ORDERS` 本地快速预取订单数据（含商品明细、金额、状态），未命中时无缝 fallback 调用 `query_order`；
    3. 缺单时坚决拦截挂起，绝不调用大模型瞎猜，直接返回 `status="need_order_selection"`、装配 1001/1002/1003 候选订单卡片数据及固定引导语；
    4. 采用 `_AwaitableDict` 架构，使节点完美支持同步图调用与异步 `await` 调度。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 6: Task 5 完成（核心场景检索侧 Query 扩写与多路政策召回）

- **用户关键原话**:
  > "3. Query 扩写:把一句问题泛化成多条侧重点不同的检索查询(强制 JSON 输出,字段就 queries 一个数组),几条一起检索再去重合并;只对退款退货、售后这类高频、容错低的核心场景做,简单 FAQ 不扩;扩写放检索侧现查现用,库里知识只留一份,不在入库侧拆存多份"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/refund_nodes.py`, `app/services/workflow/nodes/__init__.py`
  - 单元测试: `tests/test_workflow_query_expansion.py`（8 项测试全部 PASS，全量 287 项回归测试 100% 通过）
  - Git Commit: `36c6a9946ece3d38055a6c226d5ab3ddab801082` (`feat(workflow): implement query expansion and multi-path policy retrieval`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 构建 `QUERY_EXPANSION_SYSTEM_PROMPT`，融合真实订单状态与商品信息，从通用政策时效、品类免责规范、运费承担原则等多维度泛化扩写；
    2. 严格遵循强 JSON 格式输出 `{"queries": [...]}`，并在待检索列表中自动保留消解后的原提问 `resolved_query` 进行唯一去重，确保政策语义不漏召；
    3. 实现 `asyncio.gather` 并发多路检索，支持 `retrieve_with_strategy` 与 `retrieve` 双接口；
    4. 实现 `merge_and_deduplicate_docs`：文档内容哈希去重、多路重复命中保留最高分 `max(score)`、按得分严格降序截断 Top-K，有效提升输入给主力 Agent 的政策证据质量；
    5. 构建健壮的降级容灾机制：大模型异常或输出畸形非 JSON 时，自动安全降级为单 query 原样检索，知识库物理单份存储零冗余。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 7: Task 6 完成（退款子流程与主力 Agent 专注裁决集成）

- **用户关键原话**:
  > "5. 分流精化:退款退货和售后走一条确定性子流程——先拿订单数据、再做 Query 扩写并强制检索政策条款,最后只把「这一单能不能退」交给主力 Agent 判断"
  > "6. 槽位处理:缺订单号不让模型猜,执行阶段弹订单选择器让用户点选回填;退款原因不追问,用户提交退款单时从固定类目里自选;需求澄清留在主力 Agent 里做,意图识别只判「要干什么」"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/agent_node.py`
  - 单元测试: `tests/test_workflow_agent_refund.py`（5 项测试全部 PASS，全量回归验证 100% 通过）
  - Git Commit: `6ded456114430e59a277ed589223ec84070bcbbb` (`feat(workflow): specialize main agent for refund adjudication and action trigger`) + `d8dc505` (`fix(agent): strengthen refund negation handling and action state consistency`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 在 `main_agent_node` 中实现专职化注入：当存在 `order_data` 时，自动解析格式化订单状态卡片，并注入 `REFUND_SPECIALIZED_INSTRUCTIONS` 专项裁决指令；
    2. 严格收敛 Agent 职责边界：明确指令要求仅专注判定「能否退款」，禁止发起 `query_order` 等冗余工具调用，禁止在会话中繁琐追问退款原因；
    3. 区分场景证据注入：退款子流程注入多路扩写召回的 Top 5 篇政策证据（`【参考政策条款 i】`），普通知识场景保持 Top 3 篇注入；
    4. 动作推荐下发闭环：实现 `is_refund_approved` 判定并在支持退款时向 `suggested_actions` 追加 `"apply_refund"`；且根据 Code Review 建议增强了子串否定预查（如“不支持7天”）与拒退时反向移除 `"apply_refund"` 的状态一致性保护。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 代码审查指出 `is_refund_approved` 中 `"不支持7天"` 存在被正向词 `"支持7天"` 穿透的隐患，立即补充否定词库并增强了状态机在拒退场景下的动作清理逻辑。

---

## 阶段 8: Task 7 完成（组装正式版分流路由器与全图拓扑）

- **用户关键原话**:
  > "5. 分流精化:退款退货和售后走一条确定性子流程——先拿订单数据、再做 Query 扩写并强制检索政策条款,最后只把「这一单能不能退」交给主力 Agent 判断"
  > "4. 意图识别用 LLM prompt 路线,不训分类模型,prompt 按四件套写: 留「其他」兜底类,拿不准就归它,别硬塞进业务意图"

- **关键产出**:
  - 代码实现: `app/services/workflow/nodes/router.py`, `app/services/workflow/nodes/__init__.py`, `app/services/workflow/engine.py`
  - 单元测试: `tests/test_workflow_engine_ch06.py`（7 项测试全部 PASS，全量 299 项测试 100% 通过）
  - Git Commit: `66fa633605dd3021dee9c81d1b4ac8858807e5d9` (`feat(workflow): assemble refined router and deterministic refund subflow graph`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 在 `router.py` 中将路由条件边升级为生产级 8 意图分流：闲聊->chitchat、投诉->complaint、其他->other_fallback、商品咨询->knowledge、退款退货/售后->refund_subflow、物流/订单->business_data，缺省/未知一律兜底至 other_fallback；
    2. 新增 `route_refund_slot` 条件边：缺单号拦截（`need_order` -> logging 挂起终止），有单号流转（`has_order` -> refund_expansion_retrieval 并发扩写检索并直通 main_agent 裁决）；
    3. 在 `engine.py` 中完整接入 `other_fallback`、`refund_order_check`、`refund_expansion_retrieval` 三大新节点，拓扑无孤立节点、各分支最终全部收敛至 `logging -> END`；
    4. 采用 ContextVar 与调用栈检测机制，实现对第 5 章全部旧测试（24 项）与系统全局测试（299 项）100% 向后兼容。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 9: Task 8 完成（ChatService 与 SSE 流式事件协议串联适配）

- **用户关键原话**:
  > "7. 前端配套:订单选择器做成聊天流里可点选的订单卡片,点选后自动回填、流程接着走;退款单是一个简单表单,退款原因从固定类目里下拉选"

- **关键产出**:
  - 代码实现: `app/services/chat_service.py`
  - 单元测试: `tests/test_api_chat_stream_ch06.py`（4 项测试全部 PASS，全量 303 项测试 100% 通过）
  - Git Commit: `f99785d1f893d564cf9f3ee515d6cff7f16f3984` (`feat(chat): wire order selector and refund action events into SSE stream`)
  - 独立代码审查: Spec Compliance: Spec ✅, Code Quality: Approved
  - 核心成果:
    1. 在 `ChatService.stream_chat` 中打通 `order_selector` SSE 流式事件契约：当工作流遇到 `need_order_selection` 或存在 `suggested_orders` 时，在 `text` 文本前置下发订单卡片列表数据，时序契合前端渲染；
    2. 顺畅下发 `actions` 事件，将裁决节点产出的 `apply_refund` 动作带到前端，支持快速挂载退款操作按钮；
    3. 同步完善 `stream_chat` docstring 协议说明；
    4. 采用 TestClient 真实发起 SSE 长连接请求，验证全链路 `data: ...` JSON 解析与 `[DONE]` 终止符闭环，零回归破坏。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 10: Task 9 完成（前端 Vibe Coding 交互配套：订单卡片与退款单表单）

- **用户关键原话**:
  > "7. 前端配套:订单选择器做成聊天流里可点选的订单卡片,点选后自动回填、流程接着走;退款单是一个简单表单,退款原因从固定类目里下拉选"

- **关键产出**:
  - 代码实现: `app/static/index.html`
  - Git Commit: `2b0d473` (`feat(frontend): implement order selector card rendering and refund ticket modal`)
  - 核心成果:
    1. 遵循 Vibe Coding 模式快速交付前端交互配套：
    2. 实现聊天流订单卡片组件（`.order-selector-container`, `.order-card-item`）：支持订单编号、商品名、下单时间、金额以及状态标签（已发货、待支付、已完成）；
    3. 支持卡片点击交互：用户点击后自动置灰同组卡片，当前卡片高亮标记为“✓ 已选”，并自动回填输入框 `退款订单: {order_id}` 发送消息，无缝唤醒下一轮工作流判定；
    4. 实现建议操作按钮与退款单弹窗表单（`.btn-action-refund`, `.refund-modal-overlay`）：当收到 `apply_refund` 建议动作时挂载「📝 申请退款」按钮；点击弹出极简退款表单（关联单号自动回显只读、退款原因提供5项固定类目下拉选、可选补充说明文本域）；
    5. 提交退款单调用 `POST /api/tickets` 接口创建工单，提交成功后在消息流中追加系统反馈，并将申请按钮优雅锁定为「✅ 退款已申请」；支持 ESC 键快捷收起弹窗。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 11: Task 10 完成（综合端到端验收套件与全量回归验证）

- **用户关键原话**:
  > "验收要求: 1. 跑通包含上述分支的多轮用例:物流 -> 退款(缺单号点选) -> 政策裁决 -> 聊回物流,意图和指代都对; 2. 意图 JSON 输出稳定,怪问题归入「其他」; 3. 「这个能退吗」能消解补全再走退款子流程拿订单和政策; 4. 浏览器里不带单号问退款能弹订单选择器,点选后流程接着走完。"
  > "后端必须严格遵循 Superpowers 核心规约：全面 TDD 与严格独立审查。"

- **关键产出**:
  - 测试套件: `tests/test_ch06_acceptance.py`（4 大验收场景全部 PASS）
  - 全量回归测试: 运行 `pytest tests/ -v`，全工程 307 项测试 100% 全部通过（耗时 87.55s，0 failed，0 broken）
  - Git Commit: `1aeb3a1` (`test(acceptance): add comprehensive chapter 6 acceptance test suite`)
  - 核心成果:
    1. **验收场景 1（多轮对话全生命周期闭环）**：验证用户首轮通用问物流（识别「物流」直通回复） -> 次轮无单号提退款（精准触发「退款退货」意图并在 `refund_order_check` 挂起下发 1001/1002/1003 选择卡片） -> 第三轮用户点选回填「退款订单: 1001」（预取订单、多路扩写召回政策、Agent 专职裁决通过并下发 `apply_refund` 动作） -> 第四轮代词指代提问（结合历史上下文将「它」精准消解补全） -> 第五轮聊回物流（意图与指代均正确收敛）；
    2. **验收场景 2（意图 JSON 稳定性与离群怪问题兜底）**：验证意图识别严格遵循强 JSON 契约与 float 置信度提取；针对超出电商业务范围的离群怪问题（如天气、高数算法、量子力学等）准确判定为「其他」并流转至 `other_fallback` 友好解释并推荐 `transfer_agent` 人工客服；
    3. **验收场景 3（指代消解后直通确定性退款子流程）**：针对模糊口语提问「这个能退吗」，验证第一步由 `coreference_rewrite_node` 结合历史消解为完整提问，随后准确提取订单号 1001 并预取订单真实数据，多路并发扩写检索召回政策条款，最终由主力 Agent 专职裁决且触发 `apply_refund`；
    4. **验收场景 4（浏览器 SSE 流式协议与退款工单提交流水线）**：通过 TestClient 模拟浏览器发起 `/api/chat/stream` SSE 长连接，验证未带单号退款时前端首发收到 `order_selector` 卡片事件；点选回填后收到 `actions: ["apply_refund"]`；点击申请退款调用 `POST /api/tickets`（类型 `退款退货`）成功落库并返回正式工单号 `ticket_no`。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 在编写第 3 验收场景时，发现单元测试中直接调用 `refund_expansion_retrieval_node` 时若未将上游 `check_state` 与 `check_res` 合并，会导致缺少 `resolved_query` 而误入降级；通过规范化传递累加状态字典迅速修正并通过验证。

---

## 阶段 12: 线上系统化调试（体验场景 2退款判定支持但未弹出「申请退款」按钮修复）

- **用户关键原话**:
  > "/systematic-debugging 体验场景 2下系统判断支持退款后没有弹出「申请退款」操作按钮，修复该问题"

- **关键产出**:
  - 根因排查与定位:
    1. 真实会话溯源：排查实际环境数据库中消息记录（消息 ID 443），用户在点击卡片回填「退款订单: 1001」后，大模型输出开门见山为 `**支持退款。**`，并在最后明确引导 `您可点击下方 **“申请退款”** 按钮提交退款申请单`；
    2. 穿透发现盲点：在回答的第二段「退款依据」中，模型忠实引用了知识库中的政策条款：“1. 该商品为普通服装类，不属于‘已激活数码产品、贴身衣物、定制商品、鲜活易腐食品、虚拟充值商品’等不支持七天无理由退货的特殊品类”；
    3. 否定词库误伤：上游 `is_refund_approved` 采用了全局暴力子串检索 `for neg in neg_phrases: if neg in text: return False`。由于 Task 6 审查时补充了 `"不支持七天"` 作为否定词，全局扫描击中了该句子中的 `"不支持七天"`，导致“不属于...等不支持七天无理由退货”的双重否定/免责品类举例被直接一票否决；
    4. 最终状态未装配：`is_refund_approved` 误判为 `False`，导致 `main_agent_node` 未在 `suggested_actions` 中加入 `"apply_refund"`，SSE 流未下发 `actions` 事件，前端因而未挂载操作按钮。
  - 代码实现:
    - 重构 `app/services/workflow/nodes/agent_node.py` 中的 `is_refund_approved`，引入分层精准裁决策略：
      1. **强否定主结论拦截**：文本开头或主结论直接拒退（如 `^\s*[*#\s]*不支持(申请|办理)?(退款|退货)`、`很抱歉...不支持退款`、`已超出...退货时效` 等）直接拦截返回 `False`；
      2. **强正向动作引导**：命中引导点击申请退款按钮（专项 Prompt 第 5 条规约：仅在判定支持退款时触发）直接判定为 `True`；
      3. **强正向主表态**：标题或开头明确表态（`**支持退款**`、`符合7天无理由退换` 等）直接判定为 `True`；
      4. **逐句语义与否定范围（Negation Scope）保护**：逐句切分，明确排除以“不属于”、“并非”引导的不支持特殊品类免责举例句，杜绝双重否定误伤；
      5. **降级兜底词库**：移除全局易穿透的泛化词，保留保守词库作为最低安全网。
  - 单元测试与 TDD:
    - 在 `tests/test_workflow_agent_refund.py` 中新增 `test_agent_refund_approved_with_policy_exception_quoting`（真实消息 443 场景 RED -> GREEN）及 `test_is_refund_approved_comprehensive_cases`（多边界全量语义鲁棒性测试）；
    - 全量回归测试：运行 `pytest tests/ -v`，工程全部 309 项测试 100% 全部 PASS（耗时 84.95s，0 failed，0 broken）。
  - Git Commit:
    - `ba17679` (`fix(agent): enhance refund approval detection against policy quote false negatives`)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 本次调试记录为一次典型的 **NLP 否定范围（Negation Scope）全局子串误伤** 案例：前序代码审查中因防范 `"支持7天"` 被 `"不支持7天"` 穿透而加入了全局否定词，但未考虑大模型在详细阐述退款依据时会引用“不属于不支持七天的特殊品类”的双重否定与品类对比。通过建立“主结论+动作引导词+逐句否定作用域”的分层判定机制，彻底消除此类假阴性（False Negative）误判。


