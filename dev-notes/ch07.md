# 开发过程手记 (Dev Notes) - 第七章：客服三层会话上下文管理与动态预算

本文档用于追踪并留痕第七章（三层上下文结构、动态 Token 预算倒推算法、后台异步分段摘要引擎、固定顺序上下文拼装与前缀缓存保护、全链路上下文可观测日志、前端多会话侧栏与历史回载）整个开发周期的关键决策、阶段产出、沟通纠偏与技术踩坑记录。

---

## 阶段 0: Brainstorm 定稿

- **用户关键原话**:
  > "sql/ch07-ddl.sql这是表变更 我要用 Superpowers 模式让客服在一通会话里记得住前面聊过什么,把第一版的简单裁剪升级成正经的上下文管理。只管当前会话,不跨会话、不存用户画像。"
  > "1. 三层结构:按离当前这一轮的远近把历史切成三段。最近的留原文,一个字都不压;中间一层按规则截短,用户原话不动、客服答复只留开头几十个字、大块工具结果换成一行标识;最远的异步压成一段段梗概。两个锚点划边界,summary_upto_msg_id 标出摘要覆盖到哪条,layer1_from_msg_id 标出层 1 从哪条起,降级只挪这两个 id、不搬数据。梗概单开一张会话摘要表,不塞进逐条消息里"
  > "2. 摘要走后台异步:中间那层攒到超预算就在后台起一次摘要任务,把这一批压成新的一段追加进摘要表,边界追到层 1 起点,全程不阻塞用户当前这轮回复。梗概一段段攒、压完不回头重写,旧梗概只作背景给模型看、不参与合并,免得同一个事实被反复有损压缩。触发看用量不数条数,工具结果压根不落消息表,数条数看不见它涨。摘要提示词只提炼事实与诉求(问过哪款商品、报过的订单号手机号、明确诉求、还没解决的问题),对话里没出现的内容一个字不许编,寒暄闲聊不留,长度控制在几十到一两百字"
  > "3. 调模型的上下文按固定顺序拼装:system 人设、红线和工具定义打头,这一段每轮都一样;接中间那层截短过的对话,再接最近几轮原文,然后是用户当前这句话;早期梗概和检索证据合成一条挂在用户这句之后。梗概不许单独占一条 system,上游模板会把所有 system 上提合并渲染,工具定义被挤到可变内容之后,前缀缓存整段作废"
  > "4. token 预算从模型窗口倒推,不写死成常量:扣掉系统提示、检索证据、注入梗概、输出预留和安全余量这些固定开销,再扣掉当前这一轮 ReAct 的瞬时峰值,剩下的才归历史;历史预算取「想留住的轮数 × 每轮稳态占用」和「窗口实际匀得出来的」两个数里小的那个,层 1 拿七成、层 2 拿三成。启动时自检一遍,连一轮都装不下就报警。中文按字数折 token 的口径要和预算一起校准,只改一个净效果是反的"
  > "5. 对话历史落在 conversations 和 messages 两张表里,会话上下文随 LangGraph 的 State 一路贯穿:State 的 messages 字段挂 add_messages reducer,各节点只管吐新消息、框架自动按顺序并入;State 里的完整历史靠 checkpoint 落盘留着,调模型前另外整理一份裁过、带摘要的精简版递进去,完整历史和精简版各走各的、互不影响"
  > "6. 上下文可观测:每轮调模型前,把实际发出的上下文原样打进 log/app.log——主力 Agent 打 model_ctx(摘要全文 + 滑窗逐条消息 + 窗口条数 + tokens 估算),指代消解/意图识别共用的历史打 history_ctx(摘要行 + 滑窗,每轮必打,不进 Agent 的闲聊兜底轮也看得到);后台摘要任务的触发/开始/完成/跳过/失败全留痕,带覆盖边界和耗时"
  > "7. 前端多会话页面:聊天页加会话侧栏,列出该用户全部会话(新在前、首问预览、已摘要标记),点击切换并回载历史原文继续聊;「新对话」只是开新会话,旧会话仍在侧栏可切回;后端配两个只读接口(GET /api/conversations、GET /api/conversations/{id}/messages),侧栏加载失败静默降级不影响聊天"
  > "查看修改后的sql/ch07-ddl.sql，再重新进行流程"
  > 澄清问题 1 确认：“选项A”（固定项细分：1500+2500+250+500+2000+2000=8750，峰值3600，倒推5650，层1 3954，层2 1695）
  > 澄清问题 2 确认：“选项A，中文计算改为1字符1token”（目标稳态20轮，单轮500 Token，中文按1字符1token严密折算）
  > 架构方案确认：“方案1”（独立 ContextManager + 异步守护任务 + 双轨上下文透传）
  > 模块 1~5 详细设计呈现阶段：“确认”

- **关键产出**:
  - Spec 设计规范路径: `docs/superpowers/specs/2026-09-12-context-management-design.md`
  - 核心架构决议:
    1. **三层物理与逻辑边界**：`conversations` 设 `summary_upto_msg_id` 与 `layer1_from_msg_id` 两个锚点，`conversation_summaries` 单开独立表一段一行只追加；降级仅移动 ID 锚点，不搬运物理消息；
    2. **动态倒推预算器**：从 `MODEL_CONTEXT_WINDOW` 动态扣除固定项（人设/工具1500 + 检索证据2500 + 梗概预留250 + 安全余量500 + 输出2000 + 输入2000 = 8750）与 ReAct 瞬时峰值（3 步 × 1200 = 3600），与 20 轮稳态（10000）取小；层 1 占 70%（3954）、层 2 占 30%（1695）；中文折算严格执行 1 字符 = 1 Token；
    3. **前缀缓存保护拼装法**：固定 System Prompt（人设+工具 Schema）置顶，层 2 半压缩历史接层 1 原文历史，早期梗概与知识库证据统一挂在当前用户提问之后，彻底避免上游合并导致工具定义移位与 Prefix Cache 失效；
    4. **后台非阻塞分段摘要引擎**：层 2 用量超 1695 Token 触发独立协程异步提炼；旧梗概只作背景不重写，不重复合并，仅提炼事实与诉求；
    5. **LangGraph State 双轨机制**：State `messages` 维系完整还原快照与 checkpointer 落盘，调模型前现算精简视图递入；
    6. **全链路可观测日志**：输出 `[model_ctx]`、`[history_ctx]` 及摘要全生命周期日志至 `log/app.log`，支持 `grep` 瞬间直查；
    7. **前端多会话侧栏**：新会话/历史会话切换回载、首问预览、`[已摘要]` 标识，配备 `GET /api/conversations` 与 `GET /api/conversations/{id}/messages`，异常静默降级。

- **用户拒绝或纠偏**:
  - 澄清 1 中用户指令查看修改后的 `sql/ch07-ddl.sql`，确定双表结构；
  - 澄清 2 中纠偏中文 Token 折算口径为严格的 **1 字符 = 1 Token**。

- **翻车与返工**:
  - 无。

---

## 阶段 1: Plan 评审通过

- **用户关键原话**:
  > Spec 评审阶段：“确认”
  > 执行模式选择阶段：“Subagent-Driven”

- **关键产出**:
  - 实施计划文件路径: `docs/superpowers/plans/2026-09-12-context-management.md`
  - Git Commit: `922c7f8` (`docs(plan): add implementation plan for ch07 context management`)
  - 任务追踪工件: `task.md`
  - Planning 审查工件: `implementation_plan.md`
  - 10 大任务拆解与 SDD 调度机制：
    - Task 1: 数据库模型演进与 DDL 迁移脚本 (`conversations` & `conversation_summaries`)
    - Task 2: 配置项扩展与动态 Token 预算倒推算法器 (`app/config.py` & `budget.py`)
    - Task 3: 三层上下文管理器与边界推进状态机 (`manager.py`)
    - Task 4: 后台异步分段摘要引擎与事实提炼 Prompt (`summary_service.py` & `summarizer.py`)
    - Task 5: 上下文可观测日志系统与前置节点集成 (`log/app.log` & `pre_nodes.py` & `agent_node.py`)
    - Task 6: 工作流调度全链路整合 (`ChatService` & `WorkflowEngine` & `State`)
    - Task 7: 多会话后端 REST 只读接口 (`GET /api/conversations` & `GET /api/conversations/{id}/messages`)
    - Task 8: 前端多会话侧栏交互与历史回载 (`app/static/index.html`)
    - Task 9: 启动脚本与环境自愈集成 (`run.py`)
    - Task 10: 综合端到端全链路验收测试与全量回归套件 (`tests/test_ch07_acceptance.py`)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 2: Task 1 完成（数据库模型演进与 DDL 迁移脚本）

- **用户关键原话**:
  > 执行模式选择阶段：“Subagent-Driven”

- **关键产出**:
  - 代码实现:
    - `app/models/conversation.py` (新增 `summary`, `summary_upto_msg_id`, `layer1_from_msg_id` 3 个列及 1:N `summaries` 关联)
    - `app/models/summary.py` (新建 `ConversationSummary` ORM 映射模型，完全对齐 `conversation_summaries` DDL)
    - `app/models/__init__.py` (导出新模型)
    - `scripts/init_ch07_db.py` (包含引号感知 SQL 断句与 MySQL/SQLite 双方言幂等迁移执行能力)
  - 单元测试: `tests/test_ch07_models.py` (9 项单元测试 100% PASS；既有 22 项模型回归测试无缝兼容)
  - Git Commit: `8680bde0c90b024502c16ea8003b3c70496d784d` (`feat(db): implement ch07 context management models and migration script`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (架构清晰，双方言兼顾，TDD 证据确凿)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 3: Task 2 完成（配置项扩展与动态 Token 预算倒推算法器）

- **用户关键原话**:
  > 澄清问题 1 确认：“选项A”（固定项细分：1500+2500+250+500+2000+2000=8750，峰值3600，倒推5650，层1 3954，层2 1695）
  > 澄清问题 2 确认：“选项A，中文计算改为1字符1token”（目标稳态20轮，单轮500 Token，中文按1字符1token严密折算）

- **关键产出**:
  - 代码实现:
    - `app/config.py` (新增 12 项配置字段与大写属性别名，默认 128k 窗口与 20 轮稳态参数)
    - `app/services/context/budget.py` (实现精确到字符的 `estimate_tokens`、倒推公式 `calculate_context_budget` 及启动报警器 `check_budget_on_startup`)
    - `app/services/context/__init__.py` (顶层规范导出)
  - 单元测试: `tests/test_context_budget.py` (14 项测试全部 PASS，历史配置测试零回归)
  - Git Commit: `2b549886c276c95ab961311ef407ba16d6d9c258` (`feat(context): implement dynamic token budget calculator and chinese token estimator`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (公式严密，18000 窗口严格对齐 5650/3954/1695，中文 1:1 折算，自检拦截健壮)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 4: Task 3 完成（三层上下文管理器与边界推进状态机）

- **用户关键原话**:
  > 架构方案确认：“方案1”（独立 ContextManager + 异步守护任务 + 双轨上下文透传）
  > “1. 三层结构:按离当前这一轮的远近把历史切成三段。最近的留原文,一个字都不压;中间一层按规则截短,用户原话不动、客服答复只留开头几十个字、大块工具结果换成一行标识;最远的异步压成一段段梗概。两个锚点划边界,summary_upto_msg_id 标出摘要覆盖到哪条,layer1_from_msg_id 标出层 1 从哪条起,降级只挪这两个 id、不搬数据。”
  > “3. 调模型的上下文按固定顺序拼装:system 人设、红线和工具定义打头,这一段每轮都一样;接中间那层截短过的对话,再接最近几轮原文,然后是用户当前这句话;早期梗概和检索证据合成一条挂在用户这句之后。梗概不许单独占一条 system,上游模板会把所有 system 上提合并渲染,工具定义被挤到可变内容之后,前缀缓存整段作废”

- **关键产出**:
  - 代码实现:
    - `app/services/context/manager.py` (实现 `partition_messages` 逻辑切分、`apply_layer1_degradation` 锚点推进降级、`format_layer2_messages` 客服 60 字截短及工具语义单行替换、`build_model_messages` 严格前缀缓存保护组装、`build_history_context_text` 标准滑窗提取)
    - `app/services/context/__init__.py` (统一对外导出 `ContextManager` 及其核心纯函数)
  - 单元测试: `tests/test_context_manager.py` (16 项单元测试全部 PASS，上下文模块 30 项联合测试 100% 通过)
  - Git Commit: `bbe88224335300989bf67c398ab43d8d9bfd843d` (`feat(context): implement three-tier context manager and degradation state machine`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (边界 ID 纯逻辑推进、前缀缓存保护拼装绝对可靠)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 5: Task 4 完成（后台异步分段摘要引擎与事实提炼 Prompt）

- **用户关键原话**:
  > “2. 摘要走后台异步:中间那层攒到超预算就在后台起一次摘要任务,把这一批压成新的一段追加进摘要表,边界追到层 1 起点,全程不阻塞用户当前这轮回复。梗概一段段攒、压完不回头重写,旧梗概只作背景给模型看、不参与合并,免得同一个事实被反复有损压缩。触发看用量不数条数,工具结果压根不落消息表,数条数看不见它涨。摘要提示词只提炼事实与诉求(问过哪款商品、报过的订单号手机号、明确诉求、还没解决的问题),对话里没出现的内容一个字不许编,寒暄闲聊不留,长度控制在几十到一两百字”
  > 模块 4 详细设计呈现阶段：“确认”

- **关键产出**:
  - 代码实现:
    - `app/prompts/summarizer.py` (实现纯事实提炼 Prompt，内置无编造 Hard Gate、剔除寒暄、50~200 字篇幅约束与旧梗概单向隔离规范)
    - `app/services/context/summary_service.py` (实现 `SummaryService`、基于 Token 阈值判定、单机内存并发防重锁、`asyncio.create_task` 非阻塞异步调度、独立数据库 Session 管理、`conversation_summaries` 分段追加与 `conversations.summary` 投影原子更新、`[summary trigger/start/done/skip/fail]` 全生命周期留痕)
    - `app/services/context/__init__.py` (顶层统一导出)
  - 单元测试: `tests/test_context_summary_service.py` (12 项单元测试 100% PASS，全上下文套件 51 项联合测试通过)
  - Git Commit: `25fdb37519ab662d8e6051cda354c1222261c512` (`feat(context): implement async background summarization engine with fact-only prompt`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (Token 驱动非阻塞触发、分段只追加防回炉压缩设计严密)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 6: Task 5 完成（上下文可观测日志系统与前置节点集成）

- **用户关键原话**:
  > “6. 上下文可观测:每轮调模型前,把实际发出的上下文原样打进 log/app.log——主力 Agent 打 model_ctx(摘要全文 + 滑窗逐条消息 + 窗口条数 + tokens 估算),指代消解/意图识别共用的历史打 history_ctx(摘要行 + 滑窗,每轮必打,不进 Agent 的闲聊兜底轮也看得到);后台摘要任务的触发/开始/完成/跳过/失败全留痕,带覆盖边界和耗时”
  > “验收标准 4: 日志里能看到早期轮次被后台摘要任务压缩的痕迹,且摘要生成没有阻塞该轮用户回复;grep model_ctx / history_ctx 能直接看到每轮发给模型的摘要和滑窗内容”

- **关键产出**:
  - 代码实现:
    - `app/services/context/logger.py` (配置独立 `LineBufferedFileHandler` 写入 `log/app.log`，实现 `log_model_context`、`log_history_context` 及 `log_summary_lifecycle` 格式化打点器)
    - `app/services/workflow/nodes/pre_nodes.py` (在 `coreference_resolution_node` 最顶部前置打点，确保每轮必打 `[history_ctx]`，覆盖闲聊、投诉与兜底轮次)
    - `app/services/workflow/nodes/agent_node.py` (在 `main_agent_node` 调大模型前输出完整 `[model_ctx]` 包含摘要全文、滑窗及 Token 估算)
    - `app/services/context/__init__.py` (顶层统一导出)
  - 单元测试: `tests/test_context_observability.py` (9 项单元测试 100% PASS；全链路 64 项联合测试全绿)
  - Git Commit: `723793e1c1ff0883d0ebac7a16d2a83cc3519ad5` (`feat(observability): implement model_ctx and history_ctx logging in log/app.log`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (格式严密，grep 直查友好，行缓冲落盘实时安全)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 7: Task 6 完成（工作流调度全链路整合）

- **用户关键原话**:
  > “5. 对话历史落在 conversations 和 messages 两张表里,会话上下文随 LangGraph 的 State 一路贯穿:State 的 messages 字段挂 add_messages reducer,各节点只管吐新消息、框架自动按顺序并入;State 里的完整历史靠 checkpoint 落盘留着,调模型前另外整理一份裁过、带摘要的精简版递进去,完整历史和精简版各走各的、互不影响”
  > “验收标准 1: 连续聊二十轮以上,token 不爆、系统不崩”
  > “验收标准 2: 按演示配置再聊一遍... 日志里能看到完整级联:层 1 超预算就降级一批(层1 降级 X→Y),层 2 攒到超预算才起摘要(summary trigger 层2 约 N token > 预算 M),压完落成新的一段(summary done 第N段);这时再问「最开始那个订单后来怎么说」,要能靠梗概里存下的订单号和诉求答对”
  > “验收标准 3: 默认窗口下聊二十轮,不该触发任何降级和摘要。装得下就不压,压缩是成本不是美德,这一条也要验”

- **关键产出**:
  - 代码实现:
    - `app/services/workflow/state.py` (在 `AgentWorkflowState` 中扩展 `summary`, `layer2_messages`, `layer1_messages` 以及 `current_turn_tool_messages`，确保全量历史流水与精简模型入参双轨并存)
    - `app/services/workflow/nodes/agent_node.py` (`main_agent_node` 接入 `ContextManager.build_model_messages` 保证 Prefix Cache 友好拼装，且修复仅吐出本轮 `new_messages`，绝不污染 LangGraph State 历史)
    - `app/services/chat_service.py` (`stream_chat` 完成三层切分、层 1 降级推移落库、精简切片传入工作流、仅按 `current_turn_tool_messages` 派发当前轮 SSE 工具事件与落库、并在流式响应结束后基于 Token 阈值非阻塞异步触发 `trigger_async_summary`)
  - 单元与集成测试: `tests/test_chat_service_context.py` (包含多轮历史落库、层 1 预算降级、State 完整性隔离、后台非阻塞摘要触发及多轮工具调用防重复 SSE/DB 落库共 6 项测试全部 PASS)
  - Git Commit:
    - 初始提交: `44b829c1dd2dc41c8076cab1e63b8fb148fe0853` (`feat(workflow): integrate three-tier context management into chat service and workflow engine`)
    - 修复提交: `6cae9193c7d674ce858c2dd1bf094a32a67e2315` (`fix(workflow): isolate current-turn tool messages and state history in main_agent_node and chat_service`)
  - 独立代码审查:
    - Round 1: Spec ❌, Task quality: Changes requested (独立审查员敏锐指出两项 Critical 缺陷：`main_agent_node` 返回全量消息污染 State，以及多轮历史工具被重复遍历发射 SSE 和重复写入 DB)
    - Fix Round 1: All findings addressed: **YES**, Task quality: **Approved** (彻底收敛隔离)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - **翻车点 1**: 初始实现中，`main_agent_node` 直接将经过 `ContextManager.build_model_messages` 拼装好的输入 `messages`（包含 `SystemMessage`、裁剪历史和证据）放回节点返回值 `"messages": messages`。因 State 字段声明了 `add_messages` reducer，导致模型输入被错误并回 LangGraph State 与 Checkpoint，破坏了“双轨各走各的”原则；
  - **翻车点 2**: `stream_chat` 之前粗暴遍历 `final_state["messages"]`，导致第 2 轮普通对话重新遍历了第 1 轮由 Checkpoint 保持的工具调用，产生重复 `tool_start/tool_end` SSE 事件并重复向 `messages` 表写入历史工具记录；
  - **返工措施**: 立即在 `main_agent_node` 维护 `new_messages`，仅产出本轮新生成的 `AIMessage` 和 `ToolMessage`；在 State 中单独开辟 `current_turn_tool_messages` 专供 `stream_chat` 消费当前轮工具事件，并补充 `test_multiturn_tool_call_no_duplicate_sse_and_db` 测试用例，经 Re-Review 验证全量修复通过。

---

## 阶段 8: Task 7 完成（多会话后端 REST 只读接口）

- **用户关键原话**:
  > “7. 前端多会话页面:聊天页加会话侧栏,列出该用户全部会话(新在前、首问预览、已摘要标记),点击切换并回载历史原文继续聊;「新对话」只是开新会话,旧会话仍在侧栏可切回;后端配两个只读接口(GET /api/conversations、GET /api/conversations/{id}/messages),侧栏加载失败静默降级不影响聊天”
  > “验收标准 5: 前端侧栏能开多个会话对照、切回旧会话时历史完整回载、能接着聊”

- **关键产出**:
  - 代码实现:
    - `app/schemas/conversation.py` (声明 `ConversationItem` 与 `ConversationMessageItem` Pydantic 响应模型及 `ConversationListItem` 别名)
    - `app/api/routes.py` (实现 `GET /api/conversations` 与 `GET /api/conversations/{id}/messages`，支持用户隔离、时间倒序、首问前 25 字截短预览、`has_summary` 标记、全量历史时间升序加载、`tool_calls` 防御性反序列化与 404 会话校验)
  - 单元与集成测试: `tests/test_api_conversations.py` (包含空列表、用户过滤与排序、首问截取与摘要标签、404 错误、全量多角色流水回载共 6 项测试全部 PASS，API 历史测试零回归)
  - Git Commit: `347d735ac85ba443ddab122eac52cd1c295950d7` (`feat(api): implement read-only conversation list and message history endpoints`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (API 契约完备、方言适配良好、TDD 执行彻底)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 9: Task 8 完成（前端多会话侧栏交互与历史回载）

- **用户关键原话**:
  > “7. 前端多会话页面:聊天页加会话侧栏,列出该用户全部会话(新在前、首问预览、已摘要标记),点击切换并回载历史原文继续聊;「新对话」只是开新会话,旧会话仍在侧栏可切回;后端配两个只读接口(GET /api/conversations、GET /api/conversations/{id}/messages),侧栏加载失败静默降级不影响聊天”
  > “验收标准 5: 前端侧栏能开多个会话对照、切回旧会话时历史完整回载、能接着聊”

- **关键产出**:
  - 代码实现:
    - `app/static/index.html` (重构外层容器为 Flex 双栏弹性结构，新增 260px 左侧会话侧栏 `.session-sidebar`、头部“会话列表”与“+ 新对话”按钮 `#btnNewChat`、可滚动会话列表容器 `#sessionList`、`.session-item` 渲染首问预览、时间及 `[已摘要]` 标签 `.summary-badge`；JS 端实现 `loadSessions`、`createNewChat`、`selectSession` 异步回载历史消息重绘气泡，以及接口失败在 catch 中控制台记录并展示空态提示的静默降级逻辑，100% 保持既有工具卡片、评分表单与退款模态框功能)
  - 单元与集成测试:
    - `tests/test_frontend_routes.py` (包含 HTML 返回、DOM 结构标识、CSS 类样式、JS 核心交互与契约检查、接口静默降级等 5 项测试全部通过)
  - Git Commit:
    - 主功能提交: `d343ae9744e3f18679a4041b122c603eb8f484f9` (`feat(ui): add multi-session sidebar with history loading and summary badges`)
    - 细节微调: `e86d6a3` (`fix(ui): use bot avatar for historical assistant messages in sidebar reload`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Task Quality: **Approved** (审查员确认双栏布局、多会话切换、徽标与静默降级 100% 符合要求；针对审查员指出的历史助手消息头像应统一为小星 `🌟` 的细节已立即修正完成)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 10: Task 9 完成（启动脚本与环境自愈集成）

- **用户关键原话**:
  > “8. 启动与自愈:run.py 检查关系型存储时,自动执行 sql/ch07-ddl.sql 幂等演进数据库;启动时检查上下文预算是否可用,不可用立即报警;打印就绪面板”

- **关键产出**:
  - 代码实现:
    - `run.py` (在 `check_environment` 中调用 `check_budget_on_startup()` 检查 Token 预算并在不可用时报警拦截退出；在 `check_storage_readiness` 中引入并调用 `await init_ch07_db()` 自动执行 `sql/ch07-ddl.sql` 幂等 DDL 迁移；在 `print_banner` 仪表盘中展示上下文管理窗口与滑窗预算明细)
  - 单元与集成测试:
    - `tests/test_run_integration.py` (覆盖 5 项测试：正常预算自检通过并打印滑窗分配、极低窗口拦截退出、存储就绪调用 `init_ch07_db`、DDL 异常捕获拦截退出、启动仪表盘上下文状态展示，全部 PASS)
  - Git Commit: `8eab9c07e0145cb1f006f7148f87c3434f1e0062` (`feat(startup): integrate ch07 ddl migration and context budget verification into run.py`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Task Quality: **Approved** (环境预算拦截、DDL 幂等自愈执行及控制台仪表盘展示全部达标)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 11: Task 10 完成（综合端到端全链路验收测试与全量回归套件）

- **用户关键原话**:
  > “验收标准 1: 连续聊二十轮以上,token 不爆、系统不崩”
  > “验收标准 2: 按演示配置再聊一遍... 日志里能看到完整级联:层 1 超预算就降级一批(层1 降级 X→Y),层 2 攒到超预算才起摘要(summary trigger 层2 约 N token > 预算 M),压完落成新的一段(summary done 第N段);这时再问「最开始那个订单后来怎么说」,要能靠梗概里存下的订单号和诉求答对”
  > “验收标准 3: 默认窗口下聊二十轮,不该触发任何降级和摘要。装得下就不压,压缩是成本不是美德,这一条也要验”
  > “验收标准 4: 摘要过程日志齐全... 可以直接 grep 出来”
  > “验收标准 5: 前端侧栏能开多个会话对照、切回旧会话时历史完整回载、能接着聊”

- **关键产出**:
  - 代码实现与验收测试:
    - `tests/test_ch07_acceptance.py` (新增 825 行综合端到端验收套件，严格实现 5 大验收用例：`test_acceptance_criterion_1_twenty_rounds_no_overflow_no_crash` 20 轮真实交互 Token 严格控在窗口内不爆不崩；`test_acceptance_criterion_2_cascade_18000_window_lifecycle` 18000 窗口 5650/3954/1695 预算倒推、层 1 降级推移、层 2 触发摘要、新段落库、基于梗概准确召回最初订单 ORD10086 与退款诉求；`test_acceptance_criterion_3_default_128k_no_degradation_no_summary` 128k 大窗口 20 轮零降级零摘要“装得下就不压”；`test_acceptance_criterion_4_nonblocking_summary_and_observability_grep` 异步摘要非阻塞与 log/app.log grep 可观测性直查；`test_acceptance_criterion_5_multisession_sidebar_and_history_resume` 会话 A/B 对照、历史回载与跨会话隔离继续追问)
    - `tests/test_workflow_agent_refund.py` (对齐 Task 6 隔离机制，断言模型入参 SystemMessage 无退款指令)
  - 测试与回归结果:
    - `pytest tests/test_ch07_acceptance.py -v`: 5 passed in 68.01s (100% 通过)
    - 全仓库回归套件 `pytest -q`: **397 passed, 0 failed, 96 warnings in 157.39s (100% 绿灯，零回归)**
  - Git Commit: `3028795f7bbdb63b26b7a33205e5dba9471f1df7` (`test(acceptance): add comprehensive e2e acceptance test suite for ch07 context management`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Task Quality: **Approved** (5 大验收标准全部兑现，全量测试套件零回归，正式批准交付)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---
