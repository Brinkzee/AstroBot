# 开发过程手记 (Dev Notes) - 第八章：客服系统即插即用工具体系与 MCP 协议集成

本文档用于追踪并留痕第八章（统一工具注册中心、JSON Schema 校验、读写权限门禁、统一超时重试与错误分诊执行引擎、无外键高可用工具调用审计、双独立业务 MCP Server 接入、以及基于 LangGraph Interrupt 的工单人机确认流）整个开发周期的关键决策、阶段产出、沟通纠偏与技术踩坑记录。

---

## 阶段 0: Brainstorm 定稿

- **用户关键原话**:
  > "sql/ch08-ddl.sql这是需要建的表 我要用 Superpowers 模式把客服系统的工具层从几个写死的内置工具,升级成即插即用的工具系统。"
  > "1. 工具注册中心:不管内置还是 MCP 来的工具,一律带齐工具名、用途描述、JSON Schema 参数定义三样,登记进同一份工具表;内置工具服务启动时登记,MCP 工具连上 Server 动态发现、现问现拿;新工具注册进来就能被主力 Agent 用上,不改核心代码、不重启服务"
  > "2. 参数校验:工具执行前统一按 JSON Schema 校验一遍,类型不对、必填缺失、取值越界的拦下;拦下不抛异常了事,把校验错误说明包成一条工具结果回灌给模型,让它追问用户或重新组织调用"
  > "3. 权限控制:工具分只读、写两类,写操作由执行引擎把门;本系统的写操作就 create_ticket 一个,规矩是——只有客户明确要求建工单时才发起,真正执行还要等第 7 点的前端确认回来,没确认的调用引擎直接拒绝,不给模型绕过去的机会;外部 MCP 工具的用途声明是对方自己写的、不可信,能不能调(尤其写操作)只认我们这侧的权限规则,不看 Server 声明、不让模型临场判断"
  > "4. 执行引擎:所有工具调用走同一处,统一管超时、重试、错误处理、结果格式化;重试只给网络抖动这类暂时性故障,业务空结果不重试,写操作默认不自动重试(超时未必没执行,重复执行比失败更糟);错误按参数不合法、查询落空、真故障三类分诊,坏消息如实回给模型;结果只挑回答用得上的字段、内部枚举码翻成人话、序列化 JSON 中文不转义"
  > "5. 审计留痕:建 tool_audit_logs 表,每次工具调用落一条——所属会话、tool_call_id、工具名、来源是内置还是哪个 MCP Server、调用参数、结果摘要、状态(成功/失败/超时/校验拦下/权限拒绝)、错误说明、重试次数、耗时、时间;被权限拒、被校验拦的调用同样要落;审计表不挂外键,写审计失败不许反过来拦工具执行"
  > "6. MCP 接入:自建两个业务 MCP Server——物流(查物流轨迹)、售后(查在保、查退货进度),各自独立进程,Server 内部照第 2 章工具的做法随机生成 mock 数据返回,不接真实系统、不建表;客服系统作为 MCP Client 接入,拿回的工具跟内置清单合成一份,主力 Agent 一视同仁地调;物流查询由物流 MCP Server 接管,第 2 章内置的 query_logistics 从清单里下线,别留重名工具"
  > "7. 建工单确认流(带前端配套):客户明确要求建工单才走这条流,Agent 先核对信息够不够,缺问题描述这类必填项就主动追问用户,不许瞎编;凑齐后发起 create_ticket,执行引擎不直接执行,照第 6 章订单选择器的做法用 LangGraph interrupt 把工单预览(工单类型、问题描述)推给前端;前端渲染成预览卡片,带「确认提交」「取消」两个按钮:点确认,回传后端 resume 放行,落 tickets 表,回复里把工单号带给用户;点取消,不执行,这次调用按「权限拒绝」落审计;第 5 章投诉流程里前端按钮建单那条路保持不动,点按钮本身就是用户确认"
  > 澄清问题 1（工单确认流 Resume 接口）：“选项A”（专设 Resume 流式接口 POST /api/chat/resume）
  > 澄清问题 2（执行引擎门禁与外部 MCP 权限判定）：“选项A”（双重门禁 + MCP 默认只读白名单制）
  > 架构方案确认：“采用方案1”（动态统一注册中心 + 契约化执行网关 + 原生 Interrupt 交互）
  > 详细设计第 1~4 部分分节呈现：“确认”

  > “增加一项用户的意图识别分类'人工：明确要求转人工、建工单或者找客服专员跟进的（无论是否有具体问题，只要提到[转人工/建工单]诉求就归为此类意图）'，'人工'意图识别出口归为'business_data'”

- **关键产出**:
  - Spec 设计规范路径: `docs/superpowers/specs/2026-09-14-plug-and-play-tools-design.md`
  - 核心架构决议:
    1. **统一工具注册中心与 MCP 接入**：重构 `ToolRegistry`，集成 `MultiServerMCPClient`，配置物流（8001端口）与售后（8002端口）两个独立 MCP Server（Streamable HTTP）；实现“现问现拿”动态工具发现，支持 MCP Server 增删工具零重启生效；内置 `query_logistics` 从注册表下线由 MCP 物流接管；
    2. **严格参数校验器与统一执行引擎**：提取 `args_schema` 进行必填项、类型及边界校验，失败落「校验拦下」回灌模型；统一 `ToolExecutor` 调度 5.0 秒超时、仅针对网络瞬时抖动重试 1 次（写操作与业务空结果恒不自动重试），将错误分诊为参数不合法、查询落空、真故障，格式化中文输出且 `ensure_ascii=False`；
    3. **零信任权限门禁与无外键审计**：外部 MCP 工具默认一律只读；`create_ticket` 仅在用户明确提出诉求且通过前端确认放行时执行；创建 `tool_audit_logs` 表 ORM 映射，无物理外键，独立异步 Session 落盘且异常静默降级；
    4. **LangGraph Interrupt 工单人机确认流**：信息不全主动追问绝不瞎编；发起 `create_ticket` 时调用 `interrupt` 挂起，下发 `ticket_preview` SSE 事件；前端渲染预览卡片；通过 `POST /api/chat/resume` 接收确认/取消并恢复执行（确认落库 tickets、取消记权限拒绝）；保留第 5 章投诉按钮直建工单逻辑；
    5. **意图识别 9 分类扩展与路由直通**：扩展 `VALID_INTENTS` 纳入“人工”意图（包含转人工、建工单或找专员等诉求），路由出口统一映射为 `"business_data"` 直通 `main_agent_node`，确保建单全流程闭环推进。

- **用户拒绝或纠偏**:
  - 用户提出补充需求：增加意图识别 9 分类中的「人工」分类（无论是否有具体问题，只要提到[转人工/建工单]即归为此类），并将路由出口强制映射为 `"business_data"` 直通主力 Agent，已在 Spec 3.7 节完成补充修订。

- **翻车与返工**:
  - 无。

---

## 阶段 1: Plan 评审通过

- **用户关键原话**:
  > Spec 评审阶段：“确认”
  > 执行模式选择阶段：“Subagent-Driven”

- **关键产出**:
  - 实施计划文件路径: `docs/superpowers/plans/2026-09-14-plug-and-play-tools.md`
  - Git Commit: `edb6132` (`docs(plan): add implementation plan for ch08 plug-and-play tools`)
  - 任务追踪工件: `task.md`
  - Planning 审查工件: `implementation_plan.md`
  - 10 大任务拆解与 SDD 调度机制：
    - Task 1: 审计数据模型与 DDL 迁移脚本 (`tool_audit_logs` & `init_ch08_db.py`)
    - Task 2: 双独立进程业务 MCP Server 实现 (`LogisticsServer` & `AftersaleServer`)
    - Task 3: 统一工具注册中心与 MCP 客户端动态发现 (`app/tools/registry.py`)
    - Task 4: 参数 Schema 校验器与执行分诊/重试引擎 (`app/tools/executor.py`)
    - Task 5: 权限门禁与独立事务审计留痕 (`app/tools/permission.py` & `app/tools/audit.py`)
    - Task 6: 意图识别 9 分类扩展与路由直通 (`pre_nodes.py` & `router.py`)
    - Task 7: LangGraph Interrupt 工单人机确认与 Resume 流式接口 (`agent_node.py` & `routes.py`)
    - Task 8: 前端工单预览卡片交互 (Vibe Coding)
    - Task 9: 启动脚本与环境就绪集成 (`run.py`)
    - Task 10: 综合端到端全链路验收测试与全量回归套件 (`tests/test_ch08_acceptance.py`)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 2: Task 1 完成（审计数据模型与 DDL 迁移脚本）

- **用户关键原话**:
  > 执行模式选择阶段：“Subagent-Driven”

- **关键产出**:
  - 代码实现:
    - `app/models/tool_audit_log.py` (实现 `ToolAuditLog` ORM 映射模型，完全对齐 `sql/ch08-ddl.sql`，严格无物理外键，包含 `status` 与 `tool_source` 枚举)
    - `app/models/__init__.py` (导出新模型及枚举)
    - `scripts/init_ch08_db.py` (实现 MySQL/SQLite 双方言幂等迁移与引号感知 SQL 分句解析)
    - `sql/ch08-ddl.sql` (正式纳入版本管理)
  - 单元测试: `tests/test_ch08_models.py` (7 项测试 100% PASS，模型全量回归测试 27 项通过)
  - Git Commit: `c0f60b11c2cb967dc75c1a904e9ec59fe7ef284b` (`feat(db): implement tool_audit_logs model and ch08 ddl migration script`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (架构清晰、方言适配良好、无外键保证审计可用性、TDD 执行彻底)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 3: Task 2 完成（双独立进程业务 MCP Server 实现）

- **用户关键原话**:
  > “6. MCP 接入:自建两个业务 MCP Server——物流(查物流轨迹)、售后(查在保、查退货进度),各自独立进程,Server 内部照第 2 章工具的做法随机生成 mock 数据返回,不接真实系统、不建表”
  > “验收标准 2: 两个 MCP Server 单独起进程,问物流轨迹,Agent 都通过 MCP 查到数据并回答”

- **关键产出**:
  - 代码实现:
    - `scripts/mcp_logistics_server.py` (基于 FastMCP 构建 `LogisticsServer`，端口 8001，暴露 `query_logistics` 工具，支持独立命令行 Streamable HTTP 运行与 Mock 数据)
    - `scripts/mcp_aftersale_server.py` (基于 FastMCP 构建 `AftersaleServer`，端口 8002，暴露 `check_warranty`、`query_return_progress`，提供 `register_dynamic_tool` 运行时动态热插拔工具能力)
    - `requirements.txt` (补充 `mcp>=1.3.0` 与 `langchain-mcp-adapters>=0.1.0`)
  - 单元测试: `tests/test_mcp_servers.py` (7 项异步协议与业务测试 100% PASS，模型回归 14 项全绿)
  - Git Commit: `ea65388b7a4ebf90878cb8beb90c24184ffd17ba` (`feat(mcp): implement standalone logistics and aftersale fastmcp servers`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (数据无副作用浅拷贝、大小写归一化入参防御、FastMCP call_tool 与动态发现全链路测试覆盖)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 4: Task 3 完成（统一工具注册中心与 MCP 客户端动态发现）

- **用户关键原话**:
  > “1. 工具注册中心:不管内置还是 MCP 来的工具,一律带齐工具名、用途描述、JSON Schema 参数定义三样,登记进同一份工具表;内置工具服务启动时登记,MCP 工具连上 Server 动态发现、现问现拿;新工具注册进来就能被主力 Agent 用上,不改核心代码、不重启服务”
  > “6. 物流查询由物流 MCP Server 接管,第 2 章内置的 query_logistics 从清单里下线,别留重名工具”

- **关键产出**:
  - 代码实现:
    - `app/config.py` (新增物流与售后 MCP Server 端口/路径及客户端超时配置项与大写属性别名)
    - `app/tools/registry.py` (重构 `ToolRegistry`，收敛内置工具为 4 项并彻底下线 `query_logistics`；集成 `MultiServerMCPClient`；实现 `async get_all_tools()` 动态发现；支持单/多服务网络离线优雅降级；通过双通道注入 `tool_source` 与 `mcp_server` 元数据；支持 `register()` 运行时即插即用)
    - `app/tools/__init__.py` (规范化导出符号，下线内置 `query_logistics`)
    - `tests/test_tool_executor.py` (平滑对齐 4 工具断言)
  - 单元测试: `tests/test_tool_registry.py` (7 项异步与降级测试全部通过，相关 26 项测试 100% PASS)
  - Git Commit: `9f774f1fadaed3f38c5e3861ca02ae889acd0b6c` (`feat(tools): implement unified dynamic tool registry with multiserver mcp discovery`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (元数据打标健壮、单服务故障隔离细致、TDD 执行严格)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 5: Task 4 完成（参数 Schema 校验器与执行分诊/重试引擎）

- **用户关键原话**:
  > “2. 参数校验:工具执行前统一按 JSON Schema 校验一遍,类型不对、必填缺失、取值越界的拦下;拦下不抛异常了事,把校验错误说明包成一条工具结果回灌给模型,让它追问用户或重新组织调用”
  > “4. 执行引擎:所有工具调用走同一处,统一管超时、重试、错误处理、结果格式化;重试只给网络抖动这类暂时性故障,业务空结果不重试,写操作默认不自动重试(超时未必没执行,重复执行比失败更糟);错误按参数不合法、查询落空、真故障三类分诊,坏消息如实回给模型;结果只挑回答用得上的字段、内部枚举码翻成人话、序列化 JSON 中文不转义”

- **关键产出**:
  - 代码实现:
    - `app/tools/executor.py` (实现 Pydantic v2 / v1 与原生 JSON Schema 双轨参数校验，拦截必填缺失与越界取值，标记 `status="校验拦下"` 回灌提示；实现三类错误分诊：INVALID_ARGS、QUERY_MISSED、SYSTEM_FAULT；建立仅限物理连接抖动的重试白名单，针对写操作 `create_ticket` 实施严禁自动重试 Hard Gate；实现状态枚举递归人话转译；强制 JSON 序列化 `ensure_ascii=False`；规范化返回 9 字段标准结构)
    - `tests/test_tool_executor.py` (平滑对齐白名单与标准状态字段)
  - 单元测试: `tests/test_tool_executor_ch08.py` (10 项用例 100% PASS，工具执行器全套 19 项单测全部通过)
  - Git Commit: `66f58fdb10cf4215c27faafe8580c3201b61e78a` (`feat(tools): implement schema validation, retry policy, and error triage in tool executor`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (双轨校验适配完备、写操作防重试多重防御闭环、错误分诊逻辑严密)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 6: Task 5 完成（权限门禁与独立事务审计留痕）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `app/tools/permission.py` (实现 `ToolPermissionGuard`：外部 MCP 工具零信任只读，任何尝试执行写操作直接拦截并返回「权限拒绝」；唯一合法写操作工具为 `create_ticket`；`create_ticket` 实施诉求核验门禁（必须命中工单/建单/人工/专员/投诉关键词）与前端确认授权门禁 `context.get("confirmed") == True`，缺一即报「权限拒绝」)
    - `app/tools/audit.py` (实现 `AuditLogger`：独立异步事务插入 `tool_audit_logs`，超长摘要和错误信息自动截断，全流程高可用异常隔离保障审计故障绝不影响工具执行与用户应答)
    - `app/tools/executor.py` (深度装配 `permission_guard` 与 `audit_logger`，全量记录成功、校验拦下、权限拒绝、超时、失败 5 大状态)
  - 单元测试: `tests/test_tool_permission_and_audit.py` (14 项用例 100% PASS，工具执行器全套 19 项回归测试全部通过，联合 33 项用例零缺陷)
  - Git Commit: `b20144f7a77c0d0c99abc1f2007f7a601f510eea` (`feat(security): implement tool permission guard and independent non-blocking audit logger`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (零信任 MCP 拦截、双门禁写操作、独立非阻塞审计落库均已严密落地并通过全面单测)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 7: Task 6 完成（意图识别 9 分类扩展与路由直通）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `app/services/workflow/nodes/pre_nodes.py` (将 `VALID_INTENTS` 与 `ALL_INTENTS` 扩充升级为 9 分类，新增「人工」意图；在 `INTENT_FOUR_ELEMENTS_PROMPT` 中明确「7. 人工：明确要求转人工、建工单或者找客服专员跟进的（无论是否有具体问题，只要提到[转人工/建工单]诉求就归为此类意图）」，补充复合诉求及转人工典型 Few-shot，同步对齐双层模型级联与兜底规则)
    - `app/services/workflow/nodes/router.py` (在 `route_by_intent` 条件路由中，无论正式分支还是 legacy 分支，均将 `intent == "人工"` 直通路由到 `"business_data"`，确保直达 `main_agent_node` 进行后续工单人机协同交互)
  - 单元测试: `tests/test_intent_router_ch08.py` (9 项单元测试 100% PASS；既有节点与引擎测试 `tests/test_workflow_nodes.py`, `tests/test_workflow_engine.py`, `tests/test_workflow_intent_four_elements.py`, `tests/test_workflow_engine_ch06.py` 共计 30 项回归测试 100% PASS，总计 39 项测试零回归)
  - Git Commit: `c53e4e959e3b8efaa044f7709936bcac6ad4fab6` (`feat(router): add '人工' intent classification and route to business_data`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (9 分类枚举、Prompt 四件套、双层级联短路断言与直通路由完全合规，零代码异味)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 8: Task 7 完成（LangGraph Interrupt 工单人机确认与 Resume 流式接口）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `app/prompts/customer_service.py` & `app/services/workflow/nodes/agent_node.py` (在提示词中强化规约，客户要求建工单或转人工但未说明具体问题时必须主动追问详情，严禁自行编造或凭空猜测问题描述；只有在用户提供具体问题描述后才触发 `create_ticket` 工具调用，并在节点调度层设置空描述防御拦截)
    - `app/services/workflow/nodes/agent_node.py` (接入 `await default_tool_registry.get_all_tools()` 异步工具发现；接入 `default_tool_executor`；实现 `_pending_agent_states` 内存级挂起状态管理与回放保护；针对 `create_ticket` 调用 LangGraph 原生 `interrupt({"event_type": "ticket_preview", ...})` 挂起工作流；确认分支 `confirm` 注入 `confirmed=True` 调用 executor 写入 `tickets` 表并记录「成功」审计；取消分支 `cancel` 注入 `confirmed=False` 触发权限拒绝并记录「权限拒绝」审计，清洗未执行的 tool_calls 避免 OpenAI 消息协议悬挂)
    - `app/services/workflow/engine.py` (在 `WorkflowEngine` 中实现 `resume(conversation_id, action)` 方法，通过 LangGraph 原生 `Command(resume=action)` 恢复图执行)
    - `app/schemas/chat.py` (定义 `ChatResumeRequest`，使用 `action: Literal["confirm", "cancel"]` 强类型校验)
    - `app/services/chat_service.py` (在 `stream_chat` 中捕获 `__interrupt__` 发射 `ticket_preview` SSE 事件并暂停当前轮次生成；实现 `resume_chat` 异步生成器，恢复执行工作流并流式下发 `tool_start`、`tool_end`、`text` 及 `actions` 事件)
    - `app/api/routes.py` (暴露 `POST /api/chat/resume` 流式端点，输出 `text/event-stream` SSE 数据流)
  - 单元测试: `tests/test_interrupt_resume_flow.py` (共 8 项测试用例全部 100% 通过，覆盖缺失追问、工单挂起、确认建单、取消拦截与审计、SSE 流式接口、工单取消后多轮正常提问状态隔离验证、`resume_chat` 直测)
  - 回归测试: 25 项测试全部通过，0 回归
  - Git Commit:
    - 初次实现: `9e1118e9fa668f478250099e34d6a3655b00cbd4` (`feat(workflow): implement langgraph interrupt for ticket creation and resume api`)
    - 审查修复: `e2dcecb3e75b15270879a39f39dda29d4d51256c` (`fix(workflow): clean up pending resume state and sanitize cancel messages in agent node`)
  - 独立代码审查:
    - 第一次评审: **Changes requested** (指出 `_pending_agent_states` 在恢复时重新放入未及时清理，导致会话下一轮被错误重放；指出 `cancel` 分支中 AIMessage(tool_calls) 未移除导致消息悬挂)
    - 第二次评审: **Spec ✅**, **Approved** (确认状态在 `interrupt(...)` 唤醒瞬间立即彻底清理，取消分支消息历史清洗干净，入参强约束收紧，新增测试用例完备)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - **Review 返工纠偏**: 初次实现中，LangGraph 节点重入导致 `_pending_agent_states[conv_id]` 在 `resume` 恢复执行时被再次写入并未清理，导致下一轮提问被污染重放；且 `cancel` 分支遗留了无对应 `ToolMessage` 的悬挂 `tool_calls`。经独立审查拦截后，Implementer 迅速修复：在 `interrupt(...)` 返回第一时间调用 `_pending_agent_states.pop(conv_id, None)` 彻底清理，在 `cancel` 分支移除未执行的 `tool_calls` 消息，并新增 Test 7 (多轮状态隔离) 与 Test 8 (`resume_chat` 直测)，二次评审全部一次性通过。

---

## 阶段 9: Task 8 完成（前端工单预览卡片交互 - Vibe Coding）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `app/static/index.html` (采用 Vibe Coding 高保真前端美化：实现工单预览卡片 CSS 样式体系与圆角阴影微质感；在主 SSE 流处理循环中监听 `ticket_preview` 事件分发；实现 `renderTicketPreviewCard` 渲染工单类型标签、问题详情与【确认提交】/【取消】操作按钮；实现 `handleTicketDecision` 响应交互，点击后自动锁定按钮并切换为状态标签反馈；实现 `resumeChatWorkflow` 创建独立 Bot 消息气泡并以 SSE 流式请求 `POST /api/chat/resume`，打字机渲染最终应答文本并联动侧边栏与滚动条)
  - 契约测试: `tests/test_frontend_ticket_card.py` (包含 DOM 契约关键字与元素结构校验、BeautifulSoup HTML 语法有效性断言，2 项测试 100% PASS，前端全套 7 项测试全部通过)
  - Git Commit: `3dd88db522bf05a3068f80ba98d9aeec0aa27e74` (`feat(ui): implement ticket preview card and resume interaction in frontend`)
  - 审查说明: 按既定规则采用 Vibe Coding 例外规范，免除子 Agent Code Review，通过契约测试完成验收。

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 10: Task 9 完成（启动脚本与环境就绪集成 - run.py）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `run.py` (在 `check_storage_readiness` 阶段自动调用 `init_ch08_db()` 幂等演进数据库确保 `tool_audit_logs` 表就绪；集成 8001/8002 双 MCP Server 毫秒级非阻塞端口探测与连接生命周期优雅保护；调用 `default_tool_registry.get_all_tools()` 动态自检工具注册中心并分类打印内置工具与 MCP 工具拓扑；在 `print_banner` 仪表盘中增加 MCP 协议与工具注册中心状态信息行)
  - 单元测试: `tests/test_run_ch08.py` (5 项用例 100% PASS，覆盖 DDL 自动调用、迁移异常拦截、双 Server 在线探测发现、离线友好降级、仪表盘输出；历史启动自检测试 `tests/test_run_integration.py` 5 项测试全部通过，0 回归)
  - Git Commit: `61d67961a400fac9c62e216b340651bec0ee1c3a` (`feat(startup): integrate ch08 ddl migration and mcp readiness into run.py`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (快速探测防御性极高，优雅降级与恢复设计扎实，零回归保障)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - 无。

---

## 阶段 11: Task 10 完成（综合端到端全链路验收测试与全量回归套件）

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 代码实现:
    - `tests/test_ch08_acceptance.py` (全新编写 6 大端到端核心验收测试，严格对齐 Spec 与用户验收准则：AC-1 新工具零改动注册即用、AC-2 双 MCP Server 协议调用与打标、AC-3 售后 Server 热插拔免重启感知、AC-4 问建单未讲明追问补齐 + 预览卡片确认建单落库与单号回显、AC-5 预览卡片取消不建单 + 权限拒绝审计留痕 + 上下文无残留、AC-6 读超时重试 vs 写超时绝对不重试)
    - `tests/test_chat_service.py` (修复测试内对全局单例 `default_tool_executor` 的覆盖污染，显式注入独立 Mock 实例保障用例隔离)
    - `tests/test_run_integration.py` (对齐第八章 DDL 迁移流程，补充对 `init_ch08_db` 的 Mock 与断言)
    - `app/services/chat_service.py` & `app/tools/registry.py` & `app/services/workflow/nodes/router.py` (增强动态异步工具感知与既有测试环境平滑向下兼容)
  - 核心验收测试: `tests/test_ch08_acceptance.py` (6 项用例 100% PASS，耗时 66.08s)
  - 全库全量回归: `pytest -q` (**472 passed, 0 failed, 99 warnings in 412.32s**，Chapter 01 ~ 07 既有测试全部无损兼容通过，零历史回归)
  - Git Commit: `b39b599cebfeb101e4a16ceb2ea8ec5d1b702ec2` (`test(acceptance): add comprehensive e2e acceptance suite for ch08 plug-and-play tools`)
  - 独立代码审查: Spec Compliance: **Spec ✅**, Code Quality: **Approved** (6 大验收场景完整断言，系统化解决全局单例污染，472 项测试全量绿灯通过)

- **用户拒绝或纠偏**:
  - 无。

- **翻车与返工**:
  - **系统化调试与回归修复**: 在全库 472 项用例跑通回归时，定位并修复了 3 处用例间环境交互影响：(1) `test_chat_service.py` 直接 mock 了全局 `default_tool_executor` 导致后续测试审计被旁路，修复为独立 executor 注入；(2) `test_run_integration.py` 需适配第 8 章 `init_ch08_db` 迁移；(3) 遗留测试调用栈下增加安全降级。修复后全量套件一次性全部通过。

---

## 阶段 12: 终审交付与全分支收尾 (Whole-Branch Review & Polish)

- **时间**: 2026-09-14
- **用户原话**:
  > Subagent-Driven
- **关键产出**:
  - 全套任务（Task 1 ~ Task 10）100% 交付完成并核销；
  - 核心架构升级总结:
    1. **数据与合规**: `tool_audit_logs` 审计数据表与 DDL 迁移就绪，独立异步事务全量留痕，物理级无外键约束；
    2. **分布式 MCP**: 双独立进程 FastMCP Server (`LogisticsServer:8001`, `AftersaleServer:8002`)，官方 `streamable-http` 协议规范；
    3. **统一动态注册中心**: `ToolRegistry` 集成 `MultiServerMCPClient`，自动打标元数据，下线内置物流查询，支持单/双 Server 宕机毫秒级自愈与优雅降级；
    4. **安全与重试引擎**: `ToolExecutor` 实现 Pydantic/JSONSchema 双轨参数校验、错误三类分诊、瞬时抖动重试白名单、写操作绝对不重试；`ToolPermissionGuard` 强制外部 MCP 零信任只读、工单双重门禁（诉求核验 + 前端授权）；
    5. **人机协同确认**: 扩展 9 分类新增「人工」直通主 Agent，强化信息完整性反问追问；LangGraph 原生 `interrupt` 挂起推送 `ticket_preview`；实现专设流式接口 `POST /api/chat/resume` 与 `Command(resume=action)`；
    6. **前端交互**: Vibe Coding 工单预览卡片精致渲染，支持确认提交与取消互动并打字机流式呈现；
    7. **环境与工程自愈**: `run.py` 启动阶段全自动幂等迁移 DDL 并输出 MCP 拓扑状态与工具清单。
  - 全库质量指标:
    - 6 大端到端验收标准（AC-1 ~ AC-6）100% 通过（`tests/test_ch08_acceptance.py`）；
    - 全库全量回归测试 `pytest -q`：**472 passed, 0 failed**，历史 Chapter 01 ~ 07 功能零回归。
- **用户拒绝或纠偏**:
  - 无。
- **翻车与返工**:
  - 历经 Task 7 代码审查纠偏（挂起状态清理与取消分支消息清洗）与 Task 10 全库回归单例污染系统化排查，全部分支缺陷均已闭环清零。

---

## 阶段 13: 一键启动脚本全面优化与双 MCP 联动拉起 (One-Click Startup Optimization)

- **时间**: 2026-09-15
- **用户原话**:
  > 优化一键启动脚本
  > 确认（选定方案 1：全面深度优化）
- **关键产出**:
  - 代码实现:
    - `scripts/mcp_process_manager.py` (全新实现 `MCPServerManager`：端口极速探测、智能复用外部已在线 Server、未在线自动在独立子进程拉起 LogisticsServer 与 AftersaleServer、Windows `CREATE_NO_WINDOW` 避免多余控制台弹窗、输出重定向到 `log/mcp_*.log`、`atexit` 与退出清理级联终止所有托管子进程)
    - `run.py` (集成 `check_port_availability` 主服务 8000 端口占用友好拦截诊断；集成 `open_browser_async` 异步唤起默认浏览器打开 `/chat` 界面；CLI 参数支持 `--no-mcp` 与 `--no-browser`；主进程退出与 Ctrl+C 安全清理子进程)
    - `start.bat` (强制 `chcp 65001` UTF-8 编码、`where python` 环境检测、`%*` 完整参数透传、异常退出 `pause` 防黑框闪退)
    - `start.ps1` (设置 UTF-8 控制台编码、`Get-Command python` 检测、`$args` 参数透传与异常退出暂停友好交互)
    - `.gitignore` (追加忽略 `log/` 运行期子进程日志目录)
  - 单元测试与验收:
    - `tests/test_run_startup_optimization.py` (7 项专用单元测试 100% PASS，覆盖端口复用、未监听自动拉起、超时优雅处理、级联清理、端口空闲/占用检测、异步浏览器唤起、CLI 参数解析)
    - `tests/test_run_ch08.py` + `tests/test_run_integration.py` (10 项全部 PASS，0 回归)
    - `tests/test_ch08_acceptance.py` (6 大端到端验收用例全部 PASS)
    - 全库全量回归: `pytest -q` (**479 passed, 0 failed** in 509.25s，全库用例 100% 通过，0 缺陷零回归)
    - 真实执行验证: `python run.py --check-only` 成功自动拉起双 MCP 子进程并在自检完成后优雅自动释放；`python run.py --no-mcp --check-only` 成功触发安全降级运行。
- **用户拒绝或纠偏**:
  - 无。
- **翻车与返工**:
  - 无。

---
